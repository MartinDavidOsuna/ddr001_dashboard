import { api } from '@/api/client'
export interface ConstructionApiSummary {total:number;created:number;inProgress:number;executed:number;rejected:number;accepted:number;delivered:number;pendingReview:number;finished:number;completionPercent:number;photoCount:number;confirmedEvidenceCount:number}
export interface ConstructionApiMetrics {status:Array<{status:string;count:number}>;stages:Array<{stage:number;name:string;count:number}>;temporal:Array<{date:string;created:number;finished:number}>;contractors:Array<{contractorId:string;contractor:string;total:number;finished:number;inProgress:number}>;companies:Array<{crewId:string|null;crewName:string|null;total:number;finished:number;inProgress:number}>;rejection:{reviewedCount:number;rejectedCount:number;correctionCount:number;rejectionRate:number;averageCreatedToExecutedDays:number|null;averageExecutedToAcceptedDays:number|null}}
export async function getConstructionSummary(){return(await api.get<ConstructionApiSummary>('/admin/dashboard/construction/summary')).data}
export async function getConstructionMetrics(){const d=(await api.get<Omit<ConstructionApiMetrics,'temporal'>&{temporal:ConstructionApiMetrics['temporal']}>('/admin/dashboard/construction/metrics')).data;const days=new Map<string,{date:string;created:number;finished:number}>();for(const row of d.temporal){const day=days.get(row.date)||{date:row.date,created:0,finished:0};day.created+=Number(row.created);day.finished+=Number(row.finished);days.set(row.date,day)}return{...d,temporal:[...days.values()].sort((a,b)=>a.date.localeCompare(b.date))}}
